// ** Write a function that will take hour as the input parameter and will convert it into minutes and will return the result in minutes.

const hourToMinutes = (hour)=> hour*60;

const result = hourToMinutes(24);
console.log(result);
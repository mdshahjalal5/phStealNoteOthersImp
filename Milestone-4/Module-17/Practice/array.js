var numbers = [12, 32, 11, 24, 54, 56];

console.log(numbers[5]);
console.log(numbers.length)

// * Get an array Element

// * 2 no index a thaka element ta get korbo
// ? array element er position always 0 theke start hoi

console.log(numbers[2]); // retuns 11

// * set and Element

numbers[2] = 33;

console.log(numbers);

// * Finding an index 

console.log(numbers.indexOf(33));
